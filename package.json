{
  "name": "growth-engine-diagnostic",
  "version": "0.1.0",
  "description": "Scoring engine and question schema for the Growth Engine Diagnostic",
  "main": "dist/scoring.js",
  "types": "dist/scoring.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.1",
    "typescript": "^5.8.3"
  }
}
